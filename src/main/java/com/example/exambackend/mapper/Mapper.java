package com.example.exambackend.mapper;

import com.example.exambackend.DTO.RiderResponse;
import com.example.exambackend.entities.Rider;

public class Mapper {
/*
    public RiderResponse toRiderResponse(Rider rider){
        RiderResponse riderResponse = new RiderResponse();
        riderResponse.setId(rider.getId());
        riderResponse.setAge(rider.getAge());
        riderResponse.setCountry(rider.getCountry());
        riderResponse.setTeam(rider.getTeam().getName());
        riderResponse.setMountainPoints(rider.getMountainPoints());
        riderResponse.setSprintPoints(rider.getSprintPoints());
        riderResponse.setTotalTime(rider.getTotalTime());
        return riderResponse;
    }

 */

}
